import processing.core.PGraphics;
import processing.event.KeyEvent;
import processing.event.MouseEvent;

public interface ISketch extends IGui
{
    // int Width();
    // int Height();

    // void Setup(float elapsed);

    // void Update(float elapsed);

    // void Draw();
}
